def decide_next_step(session_id, intent_data):
    # later: load state, check missing fields
    return {
        "response_text": "Aap kis rajya mein rehte hain?",
        "expecting_reply": True
    }
